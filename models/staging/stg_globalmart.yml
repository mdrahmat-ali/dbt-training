version: 2

models:
  - name: stg_order
    description: one unique order per raw
    columns:
      - name: orderid
        description: the primary key for stg_orders
      - name: shipmode
        description: '{{ docs("shipmode")}}'
